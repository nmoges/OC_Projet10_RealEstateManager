package com.openclassrooms.realestatemanager.model

/**
 * Defines a photo.
 * @param photoUri : Converted [Uri] value associated with a photo.
 * @param name : name associated with a photo.
 */
class Photo(var photoUri: String, var name: String)